@using System.Globalization
@using AMAPG4.Models.User

@model AMAPG4.ViewModels.ProducerViewModel
@{
    ViewBag.Title = "Catalogue des Producteurs";
    Layout = "_Layout";
}

    <link rel="stylesheet" href="~/css/styles.css" />
    <link rel="stylesheet" href="~/css/laFerme.css" />

<div class="producer-body">
    <h2> Nos producteurs</h2>

    <p class="txt-intro">

        Ces producteurs collaborent avec Mon Panier Paysan pour créer un réseau local d'approvisionnement, permettant aux membres de bénéficier de produits frais, de qualité, et de soutenir une agriculture durable et respectueuse de l'environnement.
        Nos producteurs sont situés à moins de 50 km de notre lieu de distribution (35 km à vol d'oiseau), et les livraisons sont regroupées pour minimiser leurs trajets.
    </p>

    <div class="presentation-producer-bottom">
        <div class="left-container-producer">

            <form method="get" action="@Url.Action("Index", "Producer")">
                <!-- Search bar -->
                <input type="text" name="searchString" class="search-bar" placeholder="Rechercher un producteur..." />
            </form>
        </div>

        <!-- Producers -->
        <div class="producer-container">

            @foreach (Producer producer in Model.ProducersList)
            {
                <div class="producer-card">
                    <a href="@Url.Action("ProducerView", "Producer", new { id = producer.Id })">
                        <img class="producer-photo" src="~/images/ProducerImages/producteuroeufs.jpg" />
                    </a>
                    <div class="producer-name">@producer.Account.Name</div>
                    <div class="producer-type">@producer.ProductionType</div>
                </div>
                    
            }

        </div>

    </div>
</div>